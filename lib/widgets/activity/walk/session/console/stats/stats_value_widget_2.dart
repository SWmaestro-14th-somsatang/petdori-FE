import 'package:flutter/material.dart';

class StatsValueWidget2 extends StatelessWidget {
  const StatsValueWidget2({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('StatsValueWidget2'),
    );
  }
}
